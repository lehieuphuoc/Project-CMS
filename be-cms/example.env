# File này là mẫu cấu hình môi trường cho dự án.
# Tạo bản sao tên là .env và điền các giá trị thực tế/bí mật của bạn vào đó.

# 1. Cổng hoạt động của NestJS (Giữ nguyên)
PORT=8000

# 2. Chuỗi kết nối MongoDB Atlas
# Định dạng: mongodb+srv://<USER>:<PASSWORD>@<CLUSTER_ADDRESS>/<DATABASE_NAME>
# Ghi chú: Đảm bảo tên Database (/project-cms) được giữ lại.
MONGODB_URI=mongodb+srv://YOUR_MONGO_USERNAME:YOUR_MONGO_PASSWORD@cluster0.9goparg.mongodb.net/project-cms